export const saveUserInfo = data => (
  {
    type: 'SAVE_USERINFO',
    data,
  }
);

export const saveTravelDate = data => (
  {
    type: 'SAVE_TRAVELDATA',
    data,
  }
);

export const saveMoney = data => (
  {
    type: 'SAVE_MONEY',
    data,
  }
);

export const saveChance = data => (
  {
    type: 'SAVE_CHANCE',
    data,
  }
);

export const saveAds = data => (
  {
    type: 'SAVE_ADS',
    data,
  }
);

export const saveWxInfos = data => (
  {
    type: 'SAVE_WXINFOS',
    data,
  }
);
